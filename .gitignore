target/
.vite/

# Secrets and local env
.env
.env.*
!.env.example

# IDE / agent local overrides (never commit — often trips secret scanners)
.claude/settings.local.json

# Dependencies and build output
node_modules/
dist/

# Backend runtime data (pool registry, etc.)
backend/data/

# OS / editor
.DS_Store

# Never commit Figma export caches
**/figma-cache/
